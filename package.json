{
  "name": "claude-agent-deck",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "icons": "node scripts/make-icons.js",
    "build": "esbuild src/plugin.js --bundle --platform=node --target=node20 --format=cjs --outfile=com.tknab.claudeagents.sdPlugin/bin/plugin.js"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
